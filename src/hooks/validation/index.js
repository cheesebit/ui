export { default as useValidation } from './use-validation';

/**
 * @typedef {import('./use-validation').DispatchValidation} DispatchValidation
 * @typedef {import('./use-validation').ValidationSetup} ValidationSetup
 * @typedef {import('./use-validation').ValidationStatus} ValidationStatus
 */
