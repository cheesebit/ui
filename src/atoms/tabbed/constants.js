export const DEFAULT_WIDTH_INCREMENT = 24;
export const DEFAULT_DROPDOWN_WIDTH = 48;

export const OVERFLOW_WATCHER_SELECTOR = '.tabs > .tab';

export const OVERFLOW_OPTIONS = {
  add: () => {
    return 16;
  },
};

export const OVERFLOW_WAIT = 125;
